import styled from '@emotion/native';

export const MainContainer = styled.View({
  width: 400,
  height: 200,
  borderWidth: 1,
  // backgroundColor: '#e3ee',
});

export const FlatList = styled.FlatList({
  width: 100,
  height: 100,
  borderWidth: 1,
});

export const TestButton = styled.Pressable({
  width: 30,
  height: 30,
  borderRadius: 30 / 2,
  borderWidth: 1,
});
